apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-inquests-ui.fullname" . }}-test-connection"
  labels:
    {{- include "laa-inquests-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-inquests-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
